version: "2"

services:

  ecmascript:
    build:
      context: ../source
      dockerfile: Dockerfile
    volumes:
      - ../source/src:/usr/src/app/src
      - ../source/yarn.lock:/usr/src/app/yarn.lock
    ports:
      - 4000:4000

# networks:
  # back-tier:
  # front-tier:
# volumes:
  # db-data:
